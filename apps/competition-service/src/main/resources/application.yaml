spring:
  application:
    name: competition-service
  datasource:
    url: jdbc:postgresql://localhost:5432/competition
    driver-class-name: org.postgresql.Driver
    username: competition
    password: competition1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8092
