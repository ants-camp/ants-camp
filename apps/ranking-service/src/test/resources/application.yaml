spring:
  application:
    name: ranking-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

topics:
  trade:
    trade-succeeded: trade-succeeded-test
    ranking-update: trade-ranking-update-test
  asset:
    total-calculated: asset-total-calculated-test
